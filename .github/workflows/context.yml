name: env

on: push

jobs:
  run:
    runs-on: ubunts-lantest
    env:
      BRANCH: main
    steps:
      - run: echo "${BRANCH}"
      - uses: actions/checkout@v4
        with:
          ref: ${{env.BRANCH}}

